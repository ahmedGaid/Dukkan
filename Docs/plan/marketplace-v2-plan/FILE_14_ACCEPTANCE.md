# SESSION 14 — Acceptance + Regression + Sign-off
# Files: none (verification only; fixes go where the bug is), Docs/testing/E2E_MASTER_PROMPT.md (extend), Docs/plan/dukkan-roadmap.md (mark plan done)

---

## Full acceptance checklist (on device R5CNC0NK6ZT, live Firestore)

**A — Order details + history**
- [ ] Owner opens any order: id, datetime, status, customer name+phone (callable), address+area, COD row, subtotal/fee/total, notes, items (image/name/qty/unit/line total), driver block when assigned, full timeline.
- [ ] Timeline shows every transition with actor; old seeded orders show fallback row.

**B — Taxonomy**
- [ ] `/categories` seeded; owners cannot write it.
- [ ] Product form: dependent dropdowns, both required, edit pre-selects, free text impossible.
- [ ] Every seeded product carries `subcategoryId` + consistent parent `category`.

**C — Collections + chips**
- [ ] Owner CRUD on own collections only; delete keeps products; product in multiple collections.
- [ ] Home chips: one row, scrollable, `الكل` first, instant filter, no reload, selection survives navigation.
- [ ] Home category carries into shop page pre-filtered + clearable.

**D — Drivers**
- [ ] Courier signup → suspended profile; founder activates via console; online switch works.
- [ ] Assignment sheet lists only online + active + non-suspended + in-area + under-capacity drivers, shows areas + `n / max`.
- [ ] Race/validation: capacity, offline, wrong area, already-taken all rejected with clear Arabic messages.
- [ ] Same driver visible to two shops until capacity fills; counts increment/decrement correctly through delivered AND cancelled.
- [ ] Courier advances preparing→outForDelivery→delivered only; owner self-delivery fully intact with no driver.
- [ ] Assignment push received (or deferral logged if Worker undeployed).

**E — Commission**
- [ ] New orders snapshot bps/amounts; delivered flips payable; cancelled never payable; rate change affects only later orders.
- [ ] Finance page: six correct metrics, founder-only, delivered-only sums, COD ledger note visible.

## Regression checklist (v1 must still work)

- [ ] Customer: browse → search → favorites → cart (badge = distinct products) → checkout → طلباتي live updates → cancel pending → rate delivered.
- [ ] Owner: order desk quick actions, catalog manager, product create/edit with image, stock statuses, promos.
- [ ] Offline: home + shop render from cache; reconnect recovers.
- [ ] Both locales: full ar/en walkthrough, RTL correct, `check_i18n_parity` green, no hardcoded strings.
- [ ] All 7 seeded legacy orders open everywhere without crash (nullable-field safety net).
- [ ] `flutter analyze` 0 · `flutter test` all green · rules + indexes deployed and matching repo files.

## Micro-polish pass

- [ ] Every new empty/error/loading state designed (no bare text).
- [ ] Chip rows, sheets, tiles: tokens only, settled motion, reduced-motion honoured.
- [ ] Arabic lexicon: مندوب التوصيل, category/subcategory/collection terms each ONE canonical word, present in `Docs/Brand/BRAND.md`.
- [ ] Extend `Docs/testing/E2E_MASTER_PROMPT.md` with driver + commission steps.

## Sign-off block

Built: owner order details + statusHistory · seed-managed taxonomy + dependent dropdowns · shop collections + chips/filter carry-over · shared driver pool (areas, capacity, transaction assignment, courier shell, push) · commission ledger + founder finance page.
NOT touched: 7-status enum wire values · `core/money.dart` · Worker deploy (user-side) · `courier-role-plan/` content (banner only) · signing/release config.
Close-out: mark plan done in `dukkan-roadmap.md`, update `dukkan-status` skill (position + NEXT ACTION), commit, fresh session.

*Generated by ag-plan skill.*
